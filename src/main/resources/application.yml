
server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true

spring:
  profiles:
    active:
      - L
  jpa:
    properties:
      hibernate:
        #default_batch_fetch_size: 1000
        format_sql: true
    show-sql: false
    generate-ddl: false
    hibernate:
      #ddl-auto: create
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://notworking-db.cnnmdrrkqlx2.ap-northeast-2.rds.amazonaws.com:5432/devus_db
    username: 
    password: 
    
bokboot:
  max-source-length: 1000000


